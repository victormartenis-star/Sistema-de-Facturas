/**
 * Los esquemas de validación de este módulo viven en `../proveedores.ts`,
 * junto al resto de esquemas Zod compartidos (ver `comparativos.ts`,
 * `partes-diarios.ts`). Este fichero se conserva como redirección para no
 * romper imports existentes.
 */
export * from '../proveedores';
