/**
 * Nombres de las métricas Prometheus de la API (Fase 8). Detalle completo
 * de cada una — labels, buckets, cómo consultarlas — en la nota Obsidian
 * «Observabilidad y Telemetría».
 */
export const HTTP_REQUESTS_TOTAL = 'http_requests_total';
export const HTTP_REQUEST_DURATION_SECONDS = 'http_request_duration_seconds';
export const DB_QUERY_DURATION_SECONDS = 'db_queries_duration_seconds';
export const ACTIVE_USERS_GAUGE = 'active_users_count';
